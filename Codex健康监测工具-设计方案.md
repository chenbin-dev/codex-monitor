# Codex 健康监测工具 - 实施设计

> 状态：已实现首版（2026-08-07）

## 目标

当 Codex 因可恢复服务异常中断时，工具自动激活已校准的固定 VS Code 工作区，确认 Codex 输入框为空后输入“继续”并发送。用户不需要确认操作。

## 已实施的架构

```
~/.codex/logs_2.sqlite（只读 WAL）
        ↓ 增量扫描所有日志级别
按 process_uuid + thread_id 分类与状态机
        ↓ 延迟 30 秒并观察同线程恢复
固定校准 VS Code 窗口 → UIA 确认空输入框 → SendInput Unicode “继续”
        ↓
观察 30 秒 → 快速重试最多 3 次 → 每 5 分钟持续重试
```

## 异常与状态规则

- 自动恢复：429、502、503、504、传输超时、流中断、模型容量不足。
- 仅留历史：鉴权、余额、模型不存在、审批及无法关联 `thread_id` 的异常。
- 新错误会重置同一会话的 30 秒等待期；同线程出现完成日志会结束当前事件。
- 只有“继续”实际发送才计入重试；窗口不可操作、输入框不可读或有草稿时直接跳过，不计重试。
- 连续 3 次实际发送后仍未恢复，工具每 5 分钟继续尝试，直到该会话恢复、结束或出现不可恢复错误。

## 安全边界

- 不做 API 主动探测，不读取 `auth.json`，不复制任何密钥。
- 不保存 Codex 原始日志正文、提示词或响应；`history.db` 只保存分类摘要与动作结果。
- 不使用 `pyautogui.write()` 输入中文；使用 Windows `SendInput` 发送 Unicode，避免中文输入法依赖。
- 自动操作只针对唯一匹配的校准窗口，输入框非空时绝不清空或追加草稿。

## 运行方式

详细安装、校准和测试说明见 [README.md](README.md)。
